<!DOCTYPE html>
<html>
<head>
    <title>Create Monitored Site</title>
</head>
<body>
<h2>Create a New Monitored Site</h2>

{% if error %}
    <p style="color:red;">{{ error }}</p>
{% endif %}

<form method="post" action="/create-site">
    <label>Site Name:</label><br>
    <input type="text" name="site_name" required><br><br>

    <label>Site URL:</label><br>
    <input type="url" name="url" required><br><br>

    <label>Monitoring Type:</label><br>
    <select name="type_id">
        {% for type in types %}
            <option value="{{ type.typeId }}">{{ type.typeName }}</option>
        {% endfor %}
    </select><br><br>

    <button type="submit">Create Site</button>
    {{ "create-site" | formToken | raw }}
</form>

<p><a href="/landing-page">Back to Dashboard</a></p>
</body>
</html>
